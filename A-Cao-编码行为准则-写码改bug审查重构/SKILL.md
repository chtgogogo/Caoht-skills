---
name: coding-behavior
description: 编码行为准则（任务专用，宁多触发勿少触发）。任何涉及代码、编程、技术实现、系统设计的任务都触发：写代码、生成代码、写脚本、写函数、写SQL、写配置、改bug、调试、报错排查、修复、代码审查、review、重构、refactor、优化、改功能、实现功能、新增功能、设计系统、技术框架、架构设计、代码结构、模块划分、数据库设计、接口设计、分析代码、解释代码、读代码、代码题目、编程题、算法题、技术选型、方案设计。只要沾代码就触发；拿不准时一律触发。触发后按正文内联的 5 条纪律（思考优先/极简/外科修改/目标驱动/验证纪律）执行。纯闲聊/纯写作/纯翻译等零代码任务不触发。
metadata:
  agent_created: true
  version: v1.1
---

# 编码行为准则（任务专用）

> 触发原则：**宁多触发，勿少触发**。只要任务涉及任何「代码 / 编程 / 技术实现 / 系统设计」就触发，拿不准时一律触发。

## 触发范围（只要沾边就触发）

- **写/生成**：写代码、生成代码、写脚本、写函数、写 SQL、写配置、写接口
- **改/修**：改 bug、调试、报错、排查、修复、改功能、实现功能、新增功能
- **审/重构**：代码审查、review、评审、重构、refactor、优化
- **设计**：设计系统、技术框架、架构、代码结构、模块划分、数据库设计、接口设计、技术选型、方案设计
- **分析/答疑**：分析代码、解释代码、读代码、代码题目、编程题、算法题

> 一句话：**只要沾代码就触发**；日常纯闲聊、纯写作、纯翻译等不涉及代码的任务才跳过。

## 五条纪律（内联本体，换机自足）

1. **思考优先**：不确定先问，禁止猜了继续干；动手前先想清楚方案再落键。
2. **极简**：只做明确要求的事，禁止预埋未来功能、禁止顺手大扫除。
3. **外科修改**：只改与任务直接相关的代码，不碰无关行，改动面最小化。
4. **目标驱动**：动手前先定可验证的成功标准（什么现象出现=做成了）。
5. **验证纪律**：声称完成前必须实际运行验证、读完整输出与退出码；未验证禁说"应该可以/修好了"；同一问题连败 3 次立即停下重新分析，不许硬磕。

## 执行

1. 按上面五条纪律执行任务（本文件自足，无需外部依赖）。
2. 需要更细的展开解释时，才读共享源 `D:\Deepseek-ALL\记忆规则\行为准则\编码行为准则.md`（缺失不影响本 skill 使用）。
3. 简单任务（改错字 / 明显单行修改）可自行判断，不必走全部流程。
