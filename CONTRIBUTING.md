# CONTRIBUTING · 向本仓库新增 / 修改 skill 的规范

> 本规范对**任何人**和**任何 AI Agent** 生效。仓库根的 `AGENTS.md` 会让 Agent 进入本仓库时自动加载此要求。
> 目标：每个新 skill 进来就自带"卖点说明 + 详解页 + 双向跳转"，访客看完想用，用完知道怎么装。

## 一、文件夹与命名

1. 文件夹名用中文详细说明书名，**必须带前缀**：
   - `A-Cao-` = 基础设施级（调度/行为准则/记忆纪律），常驻加载；
   - `Cao-` = 应用级，按任务触发；
   - 无前缀目录不允许存在（历史事故：重复副本漂移）。
2. `SKILL.md` frontmatter 四件套必须齐全且 `version` 放**顶层**：
   ```yaml
   ---
   name: 调用键（英文稳定 ID，改文件夹也不改它）
   version: v1.0
   description: 一段话说清触发条件与能力（Agent 靠它决定调不调你）
   metadata:
     agent_created: true
   ---
   ```

## 二、README 收录行（卖点句式，强制）

在 README 的收录表新增一行，**卖点列必须让外行看懂"解决什么问题 + 凭什么比别的方案好"**，不许写"XX 的规则/工具"这种空话：

> ✅ 好例："让 AI 跨会话'记住该记的、忘掉该忘的'：V/R 评分+三层触发词+同坑 3 次自动升华成铁律；索引常驻 ≤2000 token——对比'记忆堆一个文件'成本恒定"
> ❌ 坏例："跨会话记忆的写入、打分、排序、召回全周期规则"

行格式：`| [短名](docs/<文件夹名>.md) | 卖点 | 版本 | [详读 →](docs/<文件夹名>.md) |`

## 三、docs/ 详解页（强制，一篇都不能少）

新建 `docs/<skill文件夹名>.md`，**模板六要素**：

1. `# 标题（name + 版本）`，紧跟顶部回链：`> [← 返回总目录](../README.md#skills-index)`
2. **一句话**：定位（不要复述 README 卖点，讲它"是什么物种"）
3. **它解决什么问题**：列 3-4 条真实痛点，写给遇到过这些痛的人看
4. **核心机制**：能力清单，每条说"机制 + 带来什么好处"
5. **和同类方案比，差在哪**：对比表（常见做法的问题 vs 本 skill 的做法），**诚实对比，禁止编造数据**
6. **3 分钟上手 / 实战绩 / 适用不适用 / 版本**

结尾双向跳转（**强制**）：

```markdown
---
[← 返回总目录](../README.md#skills-index) ｜ 下一个 → [下一篇标题](下一篇文件名.md)
```

（"下一个"按 README 表顺序链接；最后一篇的"下一个"改为"回到第一个"。）

## 四、提交前自检（不过不许交付）

```bash
py library_check.py        # 必须全部通过（exit 0）
```

体检覆盖：README/docs 收录一致、docs 回链存在、name 冲突、重复副本、frontmatter 完整性。

## 五、多宿主同步（仅基础设施类 A-Cao-）

改了 `A-Cao-` 类 skill，必须整体同步其他宿主的副本并 sha256 逐字校验，防止规则漂移。

## 六、修订本规范

规范本身要改：直接改本文件 + 同步 README 指针 + 在 commit message 里注明。别开第二个规范文件。
