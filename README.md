# Caoht Skills · AI Agent 技能工具箱

一套在真实项目里磨出来的 AI Skill 集合，可跨多种 Agent 框架复用。作者曹灏天（CaoHT）。

每一份 skill 都来自一次具体的踩坑或迭代，不是抄来的提示词。详解页里会写清楚它解决什么、凭什么比别的做法好、以及什么情况下不该用它。

---

<a id="skills-index"></a>

## 收录的 Skill

点名字看详解，看完可以一键跳回目录。

| Skill | 解决什么 | 版本 | 说明 |
|---|---|---|---|
| [A-Cao-技能调度](docs/A-Cao-技能调度-常驻路由何时调谁.md) | 装了一堆 skill 却总想不起来用。唯一入口调度器，引用清点后三态路由（点名直连 → 本地检索 → 市场兜底），支持全库穷举模式，异常自动降级，每次调用都输出可审计的透明报告 | v2.4 | [详读 →](docs/A-Cao-技能调度-常驻路由何时调谁.md) |
| [A-Cao-编码行为准则](docs/A-Cao-编码行为准则-写码改bug审查重构.md) | 治 AI 写码的老毛病：不问就猜、过度设计、没跑过就说"修好了"、遇错硬磕。思考优先、极简、外科式修改、目标驱动、验证纪律、遇错先查坑六条铁律，宁多触发勿少触发 | v1.2 | [详读 →](docs/A-Cao-编码行为准则-写码改bug审查重构.md) |
| [A-Cao-记忆纪律](docs/A-Cao-记忆纪律-跨会话存整召评分衰减升华.md) | 让 AI 跨会话记住该记的、忘掉该忘的。V/R 评分加三层触发词，同一个坑踩满 3 次自动升华成铁律，索引常驻压在 2000 token 以内 | v3.2 | [详读 →](docs/A-Cao-记忆纪律-跨会话存整召评分衰减升华.md) |
| [Cao-踩坑日志](docs/Cao-踩坑日志-经验教训沉淀.md) | 同一个坑不踩第二次。极轻索引分「根因层 + 案例层」，先认根因再翻案例，同一根因下挂多条案例显出规律，细节按需加载省 token；"能否借鉴"字段把一次性修复变成可迁移经验；命中计数——真检索、真翻看、真解决才 +1，累计 3 次自动升华进记忆铁律 | v1.3.0 | [详读 →](docs/Cao-踩坑日志-经验教训沉淀.md) |
| [Cao-多Agent团队协作](docs/Cao-多Agent团队协作文档治理-防撞车.md) | 多个 AI 一起干活不撞车。纯文档化的三层治理（决策真相源 / 协作契约 / 异步信箱），编辑锁带超时抢占，决策有生命周期，瓶颈自动交接；不依赖实时通信，天倾项目决策累积 D-01~D-83、信箱收发 225 封实战跑过（截至 2026-09-12） | v1.2 | [详读 →](docs/Cao-多Agent团队协作文档治理-防撞车.md) |
| [Cao-贴身导师](docs/Cao-贴身导师-长期陪跑逐点教学.md) | AI 家教不"假装你学会了"。四轴深度诊断 → 定制路线 → 逐点教学，5 级掌握度配画像蒸馏，跨 AI 老师无缝延续 | v1.2.0 | [详读 →](docs/Cao-贴身导师-长期陪跑逐点教学.md) |
| [Cao-Engramory记忆纪律](docs/Cao-Engramory记忆纪律-跨会话存整召.md) | 文件化长期记忆"存·整·召·指"四支柱的初代实现，带 recall / organize / check / doctor 脚本全家桶。已被记忆纪律 v3.2 取代，留作设计参考 | v1.0 | [详读 →](docs/Cao-Engramory记忆纪律-跨会话存整召.md) |
| [Cao-GitHub仓库检索提取](docs/Cao-GitHub仓库检索提取-通用提炼有用内容.md) | 把"看开源项目"变成标准流水线。gh CLI 鉴权拉取防限流、5 维价值矩阵、License 红线决策矩阵，连 API 返回 null 的坑都处理了 | v1.1.0 | [详读 →](docs/Cao-GitHub仓库检索提取-通用提炼有用内容.md) |

## 和提示词收藏夹有什么不一样

| 提示词收藏夹 | 这套 Skill |
|---|---|
| 一堆零散提示词，凑巧才好用 | 有触发词、边界、失败降级的完整规则包 |
| 换个工具就失效 | 与 Agent 框架解耦，只描述规则、流程、边界 |
| 无版本，多份副本互相漂移 | 版本化升级 + 单一事实源，改动可校验 |
| 全靠上下文硬塞 | 索引和常驻卡只放轻量信息，细节按需加载，省 token |

## 快速使用

### 安装，三步，跟具体 Agent 无关

skill 本体就是"规则 + 流程 + 边界"的纯文档（核心是每个文件夹里的 `SKILL.md`），不绑定任何框架。只要你的 Agent 支持 skills 自动发现，就能用。

1. **找到你所用 Agent 的 skills 目录。** 查它的官方文档，或者最直接的办法，问它一句："你的 skills 目录在哪？"
2. **把整个 skill 文件夹复制进去。** 保留文件夹结构，`SKILL.md` 必须在里面。
3. **重启或新开会话**，说一句该 skill 的触发词，验证是否生效。

几个常见宿主的目录位置，仅作示例，一律以各自官方文档为准：

| 宿主 | skills 目录常见位置 |
|---|---|
| ZCode | `C:\Users\<你>\.zcode\skills\` |
| Claude Code | `~/.claude/skills/` |
| Codex CLI | `~/.codex/skills/` |
| WorkBuddy | `~/.workbuddy/skills/` |
| 通用项目级 | `<项目>/skills/`（若该 Agent 支持项目级加载） |

你的 Agent 不在表里也没关系。只要它有"skills 目录加读取 SKILL.md"这套机制就通用。实在没有，`SKILL.md` 也能当提示词模板直接贴给任何 AI 用。

### 先装哪三个

建议先装基础设施三件套。它们负责回答 Agent 最基础的三个问题：什么时候该调谁（技能调度）、写代码守什么规矩（编码行为准则）、记住了什么该怎么存（记忆纪律）。其余按需选装。

---

## 命名规则与版本口径

- **A-Cao-** 前缀是基础设施级 skill（调度 / 行为准则 / 记忆纪律），各宿主常驻加载。任一副本改动，需要整体同步。
- **Cao-** 前缀是应用级 skill（教学 / 治理 / 导出 / 检索），按任务触发加载。
- 不应该存在无前缀的 skill 目录。历史上出现过无前缀重复副本造成漂移，已于 2026-09-07 清理。
- skill 的调用键以 `SKILL.md` frontmatter 里的 `name` 字段为准（文件夹名是给人看的中文说明书名）。版本号以 frontmatter 的 `version` 为唯一权威。
- 仓库级体检跑 `py library_check.py`，检查 README 与 docs 覆盖、name 冲突、重复副本、frontmatter 缺字段。

## 新增或修改 skill

任何 Agent 或人向本仓库提交新 skill 之前，必须先读 [CONTRIBUTING.md](CONTRIBUTING.md)。里面规定了文件夹命名、frontmatter 字段、README 收录行格式、docs 详解页模板（含返回目录的双向跳转链接）和提交前自检项。

仓库根目录的 `AGENTS.md` 会让 Agent 进入本仓库时自动加载这条要求。

## 仓库结构

```text
Caoht-skills/
├── README.md             # 你正在看的总目录（含索引表与跳转锚点）
├── CONTRIBUTING.md       # 新增 / 修改 skill 必读规范
├── AGENTS.md             # Agent 进入本仓库自动加载的工作规范
├── library_check.py      # 仓库级体检脚本（提交前必跑）
├── docs/                 # 每个 skill 一篇详解页
├── LICENSE
├── .gitignore
└── <skill-name>/
    ├── SKILL.md          # Agent 读取的核心指令
    ├── references/       # 可选：模板、原理、扩展资料
    └── scripts/          # 可选：可执行脚本
```

## 设计原则

**可跨 Agent 复用。** 不绑定单一工具，只描述规则、流程、边界。

**渐进式披露。** 索引和常驻卡只保留轻量信息，细节按需加载。

**证据优先。** 没有验证过就不宣称完成。重要结论要可追溯，也要允许被推翻。

**边界清晰。** 每份 skill 都写明什么时候触发、什么时候不触发、失败怎么降级。

**可维护。** 每次升级保留版本号，避免多份副本漂移。

## 许可证

[MIT](LICENSE) —— Copyright 2026 CaoHT。可商用、修改、再分发，保留版权声明即可。
