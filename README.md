# Caoht Skills · AI Agent 技能工具箱

一套在真实项目里磨出来的 AI Skill 集合，可跨多种 Agent 框架复用。作者曹灏天（CaoHT）。

每一份 skill 都来自一次具体的踩坑或迭代，不是抄来的提示词。详解页按手册规格写：解决什么故障、每个机制为什么这么设计、和同类方案比强在哪、什么情况下不该用。

---

<a id="skills-index"></a>

## 收录的 Skill

点名字看详解，看完可以一键跳回目录。说明栏是详解的浓缩版，讲解决什么、机制凭什么，末尾有详读入口。

| Skill | 版本 | 说明 |
|---|---|---|
| [A-Cao-技能调度](docs/A-Cao-技能调度-常驻路由何时调谁.md) | v2.5 | 装了一堆 skill 却总想不起来用，AI 莫名自信绕过它们。唯一入口调度器，引用清点后走固定状态机（点名直连 → 本地检索 → 市场兜底），一条消息里多个 skill 指称逐一清点不漏检，说「全面调用」进穷举模式，每次调度输出可审计的透明报告。多指称漏检事故和穷举模式都来自真实使用 [详读 →](docs/A-Cao-技能调度-常驻路由何时调谁.md) |
| [A-Cao-编码行为准则](docs/A-Cao-编码行为准则-写码改bug审查重构.md) | v2.1 | 治 AI 写码的老毛病。九条纪律各堵一个具体的坏习惯：思考优先堵不问就猜，外科修改堵顺手大扫除，验证纪律堵「应该可以了」（同一问题连败三次强制停下重新分析），证明优先要求每句「提升」都拿得出可复现证据。触发面故意放宽，沾代码就加载，宁多勿漏 [详读 →](docs/A-Cao-编码行为准则-写码改bug审查重构.md) |
| [A-Cao-记忆纪律](docs/A-Cao-记忆纪律-跨会话存整召评分衰减升华.md) | v3.5-sc1.1 | 让 AI 跨会话记住该记的、忘掉该忘的。9 层存储各配衰减速度，V/R 双轨评分排序，四层触发词保证换种说法也搜得到，同一个根因踩满 3 次自动升华成铁律进常驻区，衰减结算交给确定性脚本不靠手算。详解手册 15 章逐条讲清每个数值为什么这么定 [详读 →](docs/A-Cao-记忆纪律-跨会话存整召评分衰减升华.md) |
| [Cao-踩坑日志](docs/Cao-踩坑日志-经验教训沉淀.md) | v1.3.1 | 同一个坑不踩第二次，换马甲也认得出。根因层加案例层——中文编码一个根因炸过两次，.ps1 乱码和 git 误删目录，症状八竿子打不着，归到同一根因才显出规律；「能否借鉴」字段把一次性修复变成可迁移经验；命中计数四个条件同时成立才 +1，真救过场三次自动升华进铁律 [详读 →](docs/Cao-踩坑日志-经验教训沉淀.md) |
| [Cao-多Agent团队协作](docs/Cao-多Agent团队协作文档治理-防撞车.md) | v1.3 | 多个 AI 一起干活不撞车。纯文档三层治理零依赖：决策真相源让用户拍板一次全员生效，编辑锁带心跳超时可抢占（会话崩了不占死文件），异步信箱替代开会拉群。天倾项目 10 个角色、决策 D-01~D-83、信箱 225 封实战跑过（截至 2026-09-12） [详读 →](docs/Cao-多Agent团队协作文档治理-防撞车.md) |
| [Cao-贴身导师](docs/Cao-贴身导师-长期陪跑逐点教学.md) | v1.2.0 | AI 家教不「假装你学会了」。四轴深度诊断建画像，错题蒸馏成不变式——每条至少两个独立证据、必须能预测你下一步怎么错，没预测力的只是现象；5 级掌握度量表 L3 以下不标掌握，默认「讲回来」检验；借 AI 干活走 PEAR 循环，借了 AI 不丢理解 [详读 →](docs/Cao-贴身导师-长期陪跑逐点教学.md) |
| [Cao-GitHub仓库检索提取](docs/Cao-GitHub仓库检索提取-通用提炼有用内容.md) | v1.1.0 | 把「看开源项目」变成标准流水线。gh CLI 鉴权防匿名限流；base64 必须解码再落盘，否则几周后打开全是乱码；License 决策矩阵查表说话，连 API 返回 null 的陷阱都排了（null 时不判无 License，补查根目录 LICENSE 文件）；五维价值打分六档定级，结论强制沉淀进文档体系 [详读 →](docs/Cao-GitHub仓库检索提取-通用提炼有用内容.md) |
| [Cao-资深工程师五模板](docs/Cao-资深工程师五模板-构建审查根因架构计划.md) | v1.0 | 同一个请求两种结局：没结构拿到 demo，有结构拿到产品。构建/审查/根因/架构/计划五套交付清单自动路由；三条内嵌纪律比清单还值钱——数字不脑补（没给量级就分档给，不编精确数）、不讨好（缺陷直说带解法）、只问「缺了会白干」的问题 [详读 →](docs/Cao-资深工程师五模板-构建审查根因架构计划.md) |
| [Cao-交付流水线](docs/Cao-交付流水线-需求到交付分身编排.md) | v1.3.0 | 从模糊想法到可交付的总装流水线。十四步每步绑产出物，台账即断点存档（任何会话断了，新会话从第 N 张卡接着干），验收先于实现（开工前基线就绪，没有争论空间）；v1.2/v1.3 补上安全基线与对外口径两步，都是真实翻车换来的——外部拷问出 11 项安全实锤、对外数字全线失实，如今阻断式把关 [详读 →](docs/Cao-交付流水线-需求到交付分身编排.md) |

## 和提示词收藏夹有什么不一样

| 提示词收藏夹 | 这套 Skill |
|---|---|
| 一堆零散提示词，凑巧才好用 | 有触发词、边界、失败降级的完整规则包 |
| 换个工具就失效 | 与 Agent 框架解耦，只描述规则、流程、边界 |
| 无版本，多份副本互相漂移 | 版本化升级 + 单一事实源，改动可校验 |
| 全靠上下文硬塞 | 索引和常驻卡只放轻量信息，细节按需加载，省 token |

## 快速使用

### 安装，三步，跟具体 Agent 无关

skill 本体就是"规则 + 流程 + 边界"的纯文档（核心是每个文件夹里的 `SKILL.md`），不绑定任何框架。只要你的 Agent 支持 skills 自动发现，就能用。

1. **找到你所用 Agent 的 skills 目录。** 查它的官方文档，或者最直接的办法，问它一句："你的 skills 目录在哪？"
2. **把整个 skill 文件夹复制进去。** 保留文件夹结构，`SKILL.md` 必须在里面。
3. **重启或新开会话**，说一句该 skill 的触发词，验证是否生效。

几个常见宿主的目录位置，仅作示例，一律以各自官方文档为准：

| 宿主 | skills 目录常见位置 |
|---|---|
| ZCode | `C:\Users\<你>\.zcode\skills\` |
| Claude Code | `~/.claude/skills/` |
| Codex CLI | `~/.codex/skills/` |
| WorkBuddy | `~/.workbuddy/skills/` |
| 通用项目级 | `<项目>/skills/`（若该 Agent 支持项目级加载） |

你的 Agent 不在表里也没关系。只要它有"skills 目录加读取 SKILL.md"这套机制就通用。实在没有，`SKILL.md` 也能当提示词模板直接贴给任何 AI 用。

### 先装哪三个

建议先装基础设施三件套。它们负责回答 Agent 最基础的三个问题：什么时候该调谁（技能调度）、写代码守什么规矩（编码行为准则）、记住了什么该怎么存（记忆纪律）。其余按需选装。

---

## 命名规则与版本口径

- **A-Cao-** 前缀是基础设施级 skill（调度 / 行为准则 / 记忆纪律），各宿主常驻加载。任一副本改动，需要整体同步。
- **Cao-** 前缀是应用级 skill（教学 / 治理 / 导出 / 检索），按任务触发加载。
- 不应该存在无前缀的 skill 目录。历史上出现过无前缀重复副本造成漂移，已于 2026-09-07 清理。
- skill 的调用键以 `SKILL.md` frontmatter 里的 `name` 字段为准（文件夹名是给人看的中文说明书名）。版本号以 frontmatter 的 `version` 为唯一权威。
- 仓库级体检跑 `py library_check.py`，检查 README 与 docs 覆盖、name 冲突、重复副本、frontmatter 缺字段。

## 新增或修改 skill

任何 Agent 或人向本仓库提交新 skill 之前，必须先读 [CONTRIBUTING.md](CONTRIBUTING.md)。里面规定了文件夹命名、frontmatter 字段、README 收录行格式、docs 详解页模板（含返回目录的双向跳转链接、以及指向 `references/` `assets/` `scripts/` 的「深入阅读」资料入口）和提交前自检项。

仓库根目录的 `AGENTS.md` 会让 Agent 进入本仓库时自动加载这条要求。

## 仓库结构

> **2026-09-24 重要变更**：skill 本体已统一收归本机权威库 `D:\Deepseek-ALL\skills\`（单一事实源，避免多副本漂移）。**本仓库保留 README 与 `docs/` 详解页作为索引与说明，不再存放 skill 副本**——上方索引表的跳转全部指向 `docs/`，不受影响。
> 私有工具 `抖音聊天记录导出/` 保留在本仓库（**仅供本人使用，`.gitignore` 已排除，不入公开库**）。

```text
Caoht-skills/
├── README.md             # 总目录（索引表 → docs/ 详解页）
├── CONTRIBUTING.md       # 新增 / 修改 skill 必读规范
├── CHANGELOG.md          # 迭代留痕（每版：优化了什么 / 新增了什么 / 解决了什么）
├── AGENTS.md             # Agent 进入本仓库自动加载的工作规范
├── library_check.py      # 仓库级体检脚本（提交前必跑）
├── docs/                 # 每个 skill 一篇详解页（本仓库的实体内容）
├── LICENSE
├── .gitignore
└── 抖音聊天记录导出/        # 私有工具（已 gitignore，不入库）
```

**skill 本体在哪**：`D:\Deepseek-ALL\skills\<skill-name>\`，结构：

```text
<skill-name>/
├── SKILL.md          # Agent 读取的核心指令
├── references/       # 可选：模板、原理、扩展资料
└── scripts/          # 可选：可执行脚本
```

## 设计原则

**可跨 Agent 复用。** 不绑定单一工具，只描述规则、流程、边界。

**渐进式披露。** 索引和常驻卡只保留轻量信息，细节按需加载。

**证据优先。** 没有验证过就不宣称完成。重要结论要可追溯，也要允许被推翻。

**边界清晰。** 每份 skill 都写明什么时候触发、什么时候不触发、失败怎么降级。

**可维护。** 每次升级保留版本号，避免多份副本漂移。

## 许可证

[MIT](LICENSE) —— Copyright 2026 CaoHT。可商用、修改、再分发，保留版权声明即可。
