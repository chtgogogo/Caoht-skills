# {项目名} 团队分工与协作契约（v0.1 提案）

> **发起**：{发起角色} @ {日期}
> **状态**：提案（待各 agent 在 §7 认领、{治理主责角色} 终审转正）
> **背景**：本项目的 agent 之间没有实时通信通道，但各自产出的文档互相可见。为避免（1）多人同时改同一处造成冲突、（2）用户被甲问完乙又问同一件事，制定本契约。用「文档化契约 + 编辑锁 + 统一决策入口」替代实时沟通。

---

## 0. 核心原则

1. **文档即会议记录**：所有决策、分工、争议都写进文档，不依赖口头/对话传递。
2. **领地有责**：每个文件/模块有唯一主责 agent，别人要改先走编辑锁（§3）。
3. **用户拍板一次生效**：需要用户决定的事只进 `DECISIONS.md`，用户回复一次即对全员生效（§4）。
4. **异步信箱**：跨 agent 协作请求 / 信息传递走 `沟通/<接收人>/` 信箱（§8），不依赖实时对话。

---

## 1. 成员与角色

| 成员 | 角色 | 一句话职责 |
|---|---|---|
| 用户 | 主脑 / 最终拍板人 | 定方向、拍板决策、提供素材 |
| {角色1} | {职责简述} | |
| {角色2} | {职责简述} | |
| … | … | |

> 提示：角色由本 skill 启动时通过 AskUserQuestion 收集，请按实际团队填写。

---

## 2. 文档与模块 Ownership（主责表）

> 本表由 {发起角色} 初拟，终审后生效。各 agent 认领见 §7；非 owner 改他人区块须走 §3 编辑锁。

| 文件 / 模块 | 主责 agent | 协作 agent | 备注 |
|---|---|---|---|
| {核心设计文档} | {角色} | 全部 | 核心语义源头 |
| {代码目录} 下所有代码 | {工程角色} | | |
| `团队分工与协作契约.md` | {治理主责角色} | 全部 | 本文件 |
| `DECISIONS.md` | {决策主责角色} | 全部（登记方填写） | 统一决策入口 |

> 提示：此表按项目实际文件结构增删行；原则是"每个文件有唯一 owner"。

---

## 3. 编辑锁机制（防多人同时改一处）

**目的**：agent 看不到彼此的实时编辑，靠锁标记避免重复改同一段。

**规则**：
- 要修改**非自己主责**的文件，或对自己主责文件做**结构性/跨段落**改动，先在目标段落首部加锁标记。
- 锁标记格式（HTML 注释；代码里用对应语言注释）：
  ```
  <!-- EDIT_LOCK: <agent名> | <YYYY-MM-DD> | 改动摘要 | 状态:wip -->
  ```
- 改动完成并移除锁；若跨多段，每段独立加锁或段首统一加一处。
- 读到带 `状态:wip` 锁的段落时，**不要同时改**，先留接力说明等对方释放，或协商合并。

**示例**：
```
<!-- EDIT_LOCK: {角色} | {日期} | 改动摘要 | 状态:done -->
```

---

## 4. 统一决策入口：DECISIONS.md（解决"回复甲又回复乙"）

**铁律 1（禁止分散追问）**：任何需要用户拍板的事项，**禁止**在 agent 自己的对话里直接追问用户；必须先登记进 `DECISIONS.md` 的「待用户拍板」区。
**铁律 2（去重）**：同一决策只登记一次。登记前先搜 `DECISIONS.md` 是否已有同类条目，有则补充而非新建。
**铁律 3（一次拍板全员生效）**：用户在 `DECISIONS.md` 或对话里对某条目拍板一次，即对全员生效，任何 agent 不得再次追问同一问题。
**铁律 4（登记方负责同步）**：决策登记方负责把结论同步到相关文档，并在 `DECISIONS.md` 标记 `已决`，同时在相关文档用 `EDIT_LOCK` 标注来源。

---

## 5. 冲突裁决顺序

1. 用户拍板结论（最高优先级，覆盖一切）。
2. 文件主责 agent 的当前版本。
3. 多 agent 无法达成一致时，提交 `DECISIONS.md` 升级条目，由用户裁决。
4. 评审建议若与新决策冲突，以新决策为准，评审方负责标注「已撤销」。

---

## 6. {发起角色} 领地声明与已改动登记

**主责（他人勿重复改，除非走 §3 锁）**：
- {主责文件/模块清单}

**协作（与对应主责协商后改）**：
- {协作区块清单}

**已改动登记**（{日期}，供其他 agent 知悉，避免重复）：
- {改动1}
- {改动2}

---

## 7. 接力区（请其他 agent 补充/确认）

- **请 {角色1}**：认领 §2 Ownership 表中对应行 + 相关主责。
- **请 {角色2}**：{待确认事项}。
- **请 {治理主责角色}**：认领治理文件主责，把关本契约终审。

---

## 8. 异步信箱：沟通/ 文件夹（跨 agent 正式沟通渠道）

**规则**：
- 谁要对某人说话，就在 `<接收人>` 文件夹下生成 `X对Y.md`（X=说话人简称，Y=接收人简称）。例：{角色A}要对{角色B}说，生成 `沟通/{角色B}/{角色A}对{角色B}.md`。
- 信件建议结构：`来源 / 日期 / 主题 / 正文 / 涉及文档与引用`。
- **所有人每次开工先读自己信箱文件夹**，处理未读信件（处理完可标记或保留备查）。
- **信道分工（避免重叠）**：
  - 决策类（需用户拍板）→ `DECISIONS.md`（§4 铁律），不进信箱。
  - 文件改动类 → 编辑锁（§3）。
  - 协作请求 / 信息传递 / 任务接力 → 走信箱（本 §）。

> 详细命名约定与信件结构见本 skill 的 `references/信箱说明.md`。
