---
name: agent-failure-playbook
description: Agent 失败模式与对策库（任务专用，宁多触发勿少触发）。任何涉及设计、实现、审查、加固、排错 AI Agent 的任务都触发：设计 Agent/做一个 Agent/写 Agent 工具执行循环、给 Agent 加护栏/加限制器/加保护措施、担心无限循环/原地打转/烧 token/费用失控/越权/提示注入/上下文爆了、Agent 上线前检查、复盘 Agent 事故。命中任一关键词必触发：失败模式、无限循环、死循环、活锁、无进展、熔断器、预算控制、幂等键、状态指纹、权限门、人工确认、上下文压缩、迭代上限、重试风暴、提示注入、输出截断、guardrails、livelock、circuit breaker、idempotency key、state fingerprint、凌晨三点、工具投毒、tool poisoning、rug pull、confused deputy、致命三要素、lethal trifecta、模型漂移、评估回归、灰度发布、影子模式、多租户、审计留痕、供应商锁定、MAST。企业级场景（合规审计、多租户隔离、模型选型与退役、评估体系、灰度上线治理）同样触发。与 coding-behavior（编码纪律）叠加使用不冲突；与 Agent 无关的纯前端页面、纯脚本、纯数据分析任务不触发。
metadata:
  agent_created: true
  version: v1.1.1
  source: 提炼自 CaoHT「Agent 设计四框架问题意识」方法论（2026-09-23），六目录库结构由 CaoHT 拍板
---

# Agent 失败模式与对策库（agent-failure-playbook）

> 一句话：Agent 不是功能跑通就完了，是「知道它会怎么死」之后才算完。本库收录 **41 种死法**（失败模式，七类）、**26 种对策**、100+ 术语、11 个案例（含企业级真实事故）、3 份检查清单、2 份 AI 提问模板——设计时对着查，上线前对着勾，出事时对着排。术语收录对齐业界权威分类：UC Berkeley MAST（多 Agent 失败分类法，arXiv:2503.13657）、微软 Agentic AI 失败模式分类、MCP 安全通报（工具投毒/拉地毯/困惑代理人）、Simon Willison「致命三要素」。

## 什么时候读哪个文件（对号入座，只读命中项，省 token）

| 你正在做的事 | 读这个 |
|---|---|
| 设计/新写一个 Agent，怕想漏了坑 | references/05-checklists/设计时检查.md |
| 命中某条死法，要查根因和细节 | references/01-failure-modes/ 里对应类别那一个文件（七类：循环/错误/成本/安全/上下文/并发/治理合规） |
| 要给死法挑对策、看最小实现 | references/02-patterns/对策模式库.md |
| 说不出问题的专业名字，想搜业界解法 | references/03-terms/术语速查表.md |
| 想看别人是怎么死的（真实+构造案例） | references/04-incidents/事故案例集.md |
| 上线前最后检查 | references/05-checklists/上线前检查.md |
| Agent 已经出事（卡住/烧钱/乱删东西） | references/05-checklists/排错时检查.md |
| 怕自己有盲区，让 AI 帮着列问题 | references/06-prompts/ 两个模板 |

## 五步工作流

1. **【穷举】** 设计时打开「设计时检查」，七类死法（41 条）逐项问："我这个 Agent 会不会这样死？"
2. **【命名】** 命中的每条，到 01-failure-modes 查专业术语。**问题有了名字，才能搜到现成的药**——你不需要发明解法，只需要知道这病叫什么。
3. **【选药】** 到 02-patterns 查对策的「最小实现」，优先挑计数器级（几行代码）的先上：迭代上限、预算上限、超时，比复杂的检测算法划算十倍。
4. **【查漏】** 代码写完后，喂给 06-prompts 的模板，让 AI 扮演挑剔的 SRE/安全工程师再列一遍问题，做差距分析（哪些已有防护、哪些在裸奔）。
5. **【门禁】** 上线前过「上线前检查」的凌晨三点十问 + 八项硬门禁，产出《风险清单表》。

## 库内治理（源与视图，改库前必读）

本库一条知识只允许一个**事实源**，其余位置是**视图**：

- **01-failure-modes 是唯一事实源**：死法的现象/术语/根因/对策/检查问题的权威版本都在这里。
- **02（对策库）、03（术语表）、05（检查清单）是 01 的视图**：02 是"药柜"（对策的实现细节），03 是"字典"（术语的一句话解释），05 是"问卷"（检查问题的按场景重组）。三者头部均有派生声明。
- **04（案例）和 06（提问模板）是独立资产**：不派生自 01，各自独立维护。
- **修改顺序**：新增/修改死法 → 先改 01（编号接续该类最大号）→ 再同步 02/03/05 中受影响的条目。只改视图不改 01 = 违规。
- 向 01 追加新条目前，先到 03 术语表查重：业界已有名字的概念，优先收进现有死法的相关字段，而不是另起新条目。
- **副本落点**：母本 `D:\Deepseek-ALL\skills\Cao-失败模式与对策库（Agent Failure Mode & Playbook）\`，注册副本 `C:\Users\ASUS\.zcode\skills\agent-failure-playbook\`；改完母本后整目录覆盖注册副本并核对一致。本库不在 git 管控，大改前先备份 `SKILL.md.bak.<日期>`。

## 条目七字段格式（向 01/02 追加新条目必须遵守）

```markdown
### LOOP-1 活锁（无进展循环）
- 现象：Agent 反复调同一个工具，没有进展
- 术语：Livelock / Non-progress detection（活锁 / 无进展检测）
- 根因：模型陷入局部循环，或工具持续失败但未上报
- 对策：迭代上限 + 状态指纹做进展判据 + 熔断器
- 最坏后果：烧 token、卡死任务、上下文污染
- 检查问题：最近 N 轮状态有没有变化？同一工具同参数调了几次？
- 相关：Circuit breaker, Exponential backoff
```

编号规则：`LOOP-`(循环与终止) `ERR-`(错误与恢复) `COST-`(成本与预算) `SEC-`(安全与权限) `CTX-`(上下文与状态) `CONC-`(并发与顺序)，序号接续该文件现有最大号。

## 和近亲 skill 的边界（不抢活）

- **coding-behavior**：写码九纪律照常叠加，本库只管"Agent 会怎么死"，不管"代码怎么写"。
- **senior-engineer-playbook 模板⑤**：管"想法→计划"；本库在计划之后、写码之前做"风险审计"，两者可接力。
- **pitfall-log**：管你自己踩过的坑；本库管业界已知的 Agent 死法。新踩的 Agent 坑两边都可记：经验教训进 pitfall-log，通用的死法按七字段格式追加进本库 01。
- **缺陷诊断/调试类 skill**：普通业务 bug 走那边；只有 Agent 特有的失败模式（循环/熔断/预算/越权）走本库。

## 交付自检（收尾过一遍）

- [ ] 41 种死法（七类）逐项问过，未命中的注明"为什么不会发生"，不是跳过
- [ ] 每个命中的死法都有对策，且对策在代码/配置里有明确落点
- [ ] 上线前硬门禁全勾：八项基础 + 涉及真实用户/合规时的四项企业级附加（未上线的产物注明"仅设计评审"）
- [ ] 产出《风险清单表》：编号/现象/严重度/术语/解法/修复成本/本轮是否处理
- [ ] 本次新发现的自有坑，按七字段格式追加进 01，案例按标注规范追加进 04
