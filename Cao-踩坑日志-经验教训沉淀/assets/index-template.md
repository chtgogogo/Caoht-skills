---
name: 踩坑日志（索引 + 两层明细）
description: 共享踩坑索引；根因一个文件存 roots/，案例一个文件存 pitfalls/，本文件只留极轻索引 + 指针，省 token
type: log
created: YYYY-MM-DD
updated: YYYY-MM-DD
---

# 踩坑日志（索引 + 两层明细）

> 自我学习账本，**统一单一事实源**。遇到坑 / 错 / bug / 特殊困难及最终解法 → 先在 `roots/` 归因（同根因就挂上去），再追加一条案例。

> 为什么这样做：记录越多，若全内联在一个文件里，每次阅读都会全量加载 → 越来越费 token。解法：**根因进 `roots/`、案例进 `pitfalls/`，一条一个文件**；本索引只留「根因表 + 一行案例摘要」。平时只读索引；检索时先看根因，再看案例。

## 🧭 检索顺序（先根因，后案例）

1. 读本索引 → 2. 先扫「根因索引表」，看识别信号有没有对得上的 → 3. 对上就读 `roots/RT-*.md` 拿通用对策 → 4. 不够再翻该根因下的具体案例找灵感 → 5. 解决了就给**真正帮上忙的那个** +1

## 命中计数（+1 的四个条件）

① 真的检索了 ② 真的翻看了具体记录 ③ 从里面获得启发并用于解决 ④ 问题真的解决了。
**事后发现「同根因」不算命中**——没有检索、没有翻看、没有获益。

## 索引格式

**① 根因索引表**

| 根因 | 一句话 | 案例数 | 命中 |
|---|---|---|---|
| [RT-001](roots/RT-001-<slug>.md) | 一句话根因 | 2 | 🔥0 |

**② 案例清单（按根因分组，组内新 → 旧）**

### RT-001 <一句话根因>

| 日期 | 记录者 | 标签 | 一句话标题 | 命中 | 文件 |
|---|---|---|---|---|---|
| YYYY-MM-DD | [记录者] | 标签1,标签2 | 一句话说清症状或解法 | 🔥0 | [详情](pitfalls/YYYY-MM-DD-<slug>.md) |

## 两层文件结构

- **根因文件**：`roots/RT-<三位编号>-<slug>.md`（模板见 `assets/root-template.md`）
- **案例文件**：`pitfalls/YYYY-MM-DD-<slug>.md`（模板见 `assets/entry-template.md`）

---

## 记录区（新 → 旧）

（在此追加：先更新根因表的案例数，再在对应根因分组下加一行）
