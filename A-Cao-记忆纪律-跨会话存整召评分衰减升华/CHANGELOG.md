# memory-scoring-lifecycle CHANGELOG（迭代留痕）

> 本文件为该 skill 的迭代文档（铁律 6）。此前版本未建 CHANGELOG，自 v3.5-sc1 起补建；v3.5 及更早的规则迭代史见权威源 `D:\Deepseek-ALL\记忆规则\00_记忆系统总纲.md` §14。

## v3.5-sc1（2026-09-23）自包含改造 + Probe 纪律

**优化了哪些地方：**
- 原版 SKILL.md 是"摘要+指针"结构：三记五不记、V/R 打分量表、检索预算、记录模板等核心定义全部只存在于 D 盘权威源 00–12 号文档，skill 本体离开权威源即不可执行。
- 修复版本漂移：E 盘 store 实践中已有"写入先取号防撞车"补丁（撞车实发 3 次后加，R-004），但 skill 规则与权威源均未收录——本次回灌进核心流程 §5。

**增加了什么功能：**
- **自包含**：7 类核心定义（store 结构/TTL 档、三记五不记、双轨评分量表、加扣分表、惰性衰减、检索协议+硬预算、冲突处理、升华机制、记录/索引/CORE 模板、体检清单、冷启动步骤、引导卡模板）全部内嵌，单文件即可完整执行；外部指针降级为"扩展阅读"，失效不影响执行。
- **Probe 纪律（§5，新增）**：写前取号与实地核实、用前核实、指针必须自带降级路径、值不值得 probe 的判据（易漂移必核/稳定事实不核）、probe 预算（每条 ≤1 次工具调用）、失效处理（superseded 不删+改写）。
- **体检新增第 ⑥ 项漂移抽查**：定期抽 3 条环境类记忆实地核实，过期即标 superseded 并写修正版。
- **冷启动降级路径**：脚本拿不到时手工按 7 条核心跑，不阻塞新 agent 接入。

**解决了什么问题：**
- 指针断链风险：D 盘共享源若移动/改名/盘符变化，旧版 skill 立即残废——现在核心流程零外部依赖。
- 记忆传染错误：此前写入只凭会话观察（相当于微软论文里的 trajectory-only 策展），环境类记忆（路径/数字/版本）可能一落笔就是过期/错误的；probe 纪律把核实提前到写入/使用时刻，不再等下游任务踩坑。
- 规则与实践漂移：store 侧补丁（取号）此前无法回流到只读的权威源，导致"做的不一样、写的还是旧"——现以 skill 封装层（sc 版本号）承载增量，不动权威源。
- 触发安全：frontmatter 的 name/description 逐字未动，useskill 调度与自动触发链不受影响。

**同步状态：**
- 改动落点：注册副本 `C:\Users\ASUS\.zcode\skills\memory-scoring-lifecycle\`（生效版）。
- D 盘权威库副本（`A-Cao-记忆纪律-跨会话存整召评分衰减升华`）与 E 盘 MEMORY.md 引导卡**未动**，是否同步待用户拍板。
- 改造前原文备份：`E:\Zcode-work\提炼skill\备份-改造前原文\SKILL.md.bak_自包含改造前_20260923.md`；分析笔记：`E:\Zcode-work\提炼skill\` 下同名分析文档。

## v3.5-sc1 追记（2026-09-23 同日）
- 新增「兄弟 skill：pitfall-log（踩坑日志）」节：互认、查坑时机（反复/连败 3 次强制翻坑）、存哪边判据、升华回流（RT-xxx 进 CORE）、巡检保障。踩坑 skill 已注册，触发链打通。

## v3.5-sc1.1（2026-09-23）权威库回流 + 双副本终态一致

**做了什么：**
- sc1 自包含版（SKILL.md + CHANGELOG）回流权威库 `A-Cao-记忆纪律-跨会话存整召评分衰减升华`，双副本 md5 逐字一致；
- §0.5 立规「**权威库永远保持最优版**——注册副本优化验证通过后及时回流，不得停留在旧版」（用户令 2026-09-23）；
- patrol_check 的已知分叉白名单清除（分叉归零后不再需要）。

**解决了什么：**
- 权威库不再是旧版（旧版缺 probe 纪律、缺自包含定义、缺取号步骤），彻底消除"权威库反而是最差的副本"的倒挂；
- 全系统唯一一条已知 skill 分叉清零；"改动先落注册副本、权威库走流程"造成的规则层滞后从此有制度性出口。

**怎么解决：**
- 用户拍板后 cp 同步 + md5 校验 + patrol 复跑验证 PAIR 全绿。

**同步状态（终态）：**
- C 盘注册副本 = D 盘权威库副本（逐字一致，patrol 每日把关）；E 盘 MEMORY.md 引导卡维持旧 7 条（用户未拍板动它，功能不受影响——skill 已自包含）。
